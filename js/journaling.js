function Entry(title, entry) {
  this.title = title;
  this.entry = entry;
}

Entry.prototype.wordCount = function() {
  var output = [];
}







var vowels = ["a", "e", "i", "o", "u"];

//i = title.length or whatever
//count down i for character in title array
//loop through array for each vowel array object, adding each match to another array (wordVowelsArray)
//amount of vowels = wordVowelsArray.length


exports.journalingModule = Entry
